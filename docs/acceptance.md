# 当前验收 · 0.5.0

新增共享角色库多进程并发/进程崩溃恢复、整批写入失败、备份校验/同版本冲突/预览后冲突、偏好写入时比较、浏览器备份往返和三组 daily 动作分帧与收尾检查。原有明暗/窄屏/拖动/角色/台词/空挥/多会话回归保留。

实际 Linux arm64 两组 Node 干净依赖安装、构建和单元测试已执行；macOS 原生 Host 测试使用最终插件 tarball，包含备份恢复和旧版升级。持续运行时长及堆/DOM 指标以本轮报告为准，不把目标时长当成执行结果。最终汇总见 `dist/verification-0.5.0.json`。

源码包现在包含 pnpm-workspace.yaml；该文件允许已锁定的 esbuild 构建步骤，是干净环境复现所需配置。

以下均为历史版本验收记录。

# 当前增量验收 · 0.4.3

鞭策开关在快捷/完整设置同步，切换后及时停止鞭子与反应，刷新和换角色保留偏好；不支持反应素材时仍能关闭总开关。普通工作池排除外部 near-miss；明确素材预览继续可用。多会话验证延续 0.4.2 的 10 项专项与真实 Host 双任务检查。最终次数和安装证据以 `dist/verification-0.4.3.json` 为准。

以下为历史验收，不表示每次修订均重复五分钟 soak 或跨平台检查。

# 0.4.1 增量验收

本轮检查设置弹窗滚动到底后关闭按钮的可见性与命中，覆盖桌面/390 px 窄屏及深色主题；角色测试改用成年版，验证新版安装包不含已删除角色素材。下文是上一轮动画层验收，五分钟 soak 未因纯布局修改重复执行。

# 0.4.0 验收说明

最终执行结果以 `dist/verification-0.4.0.json` 及报告为准；环境和局限见 [compatibility.md](compatibility.md)。

| 项目 | 验证行为 |
| --- | --- |
| 速度与任务状态 | 20/100/400 自适应、冷启动、去重、工具/首段等待不处罚、确认/终态优先于残留工具标签 |
| 高优先级状态 | 确认立即停止工作，播放专属伸手动作后保持；失败与成功分别回退，取消安静休息 |
| 调度 | 普通循环收尾、一次性动作不重复重启、最近 3 次避免重复、变速时播放时钟连续、后台恢复不追赶 |
| 新美术 | 新增 16 个绘制姿态，累计 60；29 类动作含 5 种举牌。阅读/修理/安静/确认在真实 Canvas 检查透明底、取帧和站位 |
| 空挥 | 绳子绘制共享，角色与绳子分区裁剪；外部角色关闭互动/改变档位后 bounds 完全一致 |
| 设置 | 角色可用动作数量、空挥缺素材原因；JSON 编排移入高级区；角色选择和原有偏好保留 |
| 预览 | 内置 29 类和成年版 8 组均可预览，不修改当前使用角色或日常设置 |
| 浏览器 | 拖动/折叠/恢复、单卡布局、390 px 窄屏、深浅主题、系统减弱动态、活泼默认、显式安静 |
| Host | 最终 tarball 隔离安装与卸载；从 0.3.2 保存参数/角色后升级；本地测试模型经真正 DSH 流驱动 |
| 生命周期 | 五分钟真实时间循环，持续检查 DOM/Canvas 和 JS 堆；另有十分钟虚拟时间安静策略回归 |
| 分发 | 0.4.0 插件/源码、1.0.1 制作 Skill；tarball 和 Skill ZIP 内统一使用 dsh-bigfish-pet-maker 名称 |

素材数量是独立图集姿态数量，动作会复用帧和程序道具；不把 29 类动作描述为 29 套独立绘制动画。少数低频动作仍使用单张姿态与程序动画。举牌采用现有关键姿态编排停留/回收，未宣称五种都新增绘制了完整放下动作。
